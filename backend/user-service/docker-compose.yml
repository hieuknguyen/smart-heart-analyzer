version: "3.9"

services:
  db:
    image: mysql:8.0
    container_name: mysql_db
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
    ports:
      - "3309:3306"   # external 3309 → internal 3306
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-p${MYSQL_PASSWORD}"]
      interval: 5s
      timeout: 10s
      retries: 5
      start_period: 30s
    volumes:
      - mysql_data:/var/lib/mysql

  api:
    build: .
    image: hieuknguyen/chat-api:latest
    container_name: fastapi_app
    depends_on:
      db:
        condition: service_healthy
    environment:
      MYSQL_HOST: db              # ← hostname đúng
      MYSQL_PORT: 3306            # ← port bên trong container
      MYSQL_USER: root            # ← vì bạn không tạo user khác
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      ENVIRONMENT: production
    ports:
      - "8000:8000"
    restart: unless-stopped

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: phpmyadmin
    depends_on:
      db:
        condition: service_healthy
    environment:
      PMA_HOST: db                # ← dùng "db"
      PMA_PORT: 3306
      PMA_USER: root
      PMA_PASSWORD: ${MYSQL_PASSWORD}
    ports:
      - "8080:80"

volumes:
  mysql_data:
